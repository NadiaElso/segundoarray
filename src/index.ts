//let dimension: number=Number(prompt("Ingrese la dimension"));
let nombres: string[] = new Array(dimension);
let index:number;

for (let index = 0; index < nombres.length; index++) {
  //nombres [index] = prompt("Ingrese un nombre que quiere poner en el lugar",${index},":");
 
}

for (let i = 0; i < nombres.length; i++) {
  console.log("La persona que ingresó en la posicion",${i}"es":" ``${n});
  
}